<div class="container round-layout">
  <div class="game_banner">
    <h1><%= @round.game.name %></h1>
  </div>
  <%= render 'questions/show', question: @question, round: @round, answer: @answer %>
</div>
